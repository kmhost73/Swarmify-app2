{
  "name": "swarmify-app",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "scripts": {
    "start": "esbuild src/index.js --bundle --servedir=public --outfile=public/bundle.js --loader:.js=jsx",
    "build": "esbuild src/index.js --bundle --outfile=build/bundle.js --loader:.js=jsx --minify"
  },
  "devDependencies": {
    "esbuild": "^0.20.0"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
